from setuptools import setup, find_packages

setup(name = "census-income",
      version = "0.0.1",
      author = "siva",
      author_mail = "siva@gmal.com",
      package = find_packages(),
      install_requires = ["pandas","numpy"]  
    
)